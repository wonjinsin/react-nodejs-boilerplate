import React from "react";
import styles from "@css/common/header.module.css";

const HeaderComponent = () => {
  return <header className={styles.header}>Header Area</header>;
};

export default HeaderComponent;
